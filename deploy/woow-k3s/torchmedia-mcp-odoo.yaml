# Instance values for the mcp-odoo release in namespace torchmedia.
# Generated from the live objects; every field was read off the cluster.
#
# SAFETY: never set `namespace:` here - placement is `-n` only.

storageClassName: longhorn

baseName: mcp-odoo
imagePullSecrets:
  - name: ghcr-pull

service:
  ports:
    - name: admin
      port: 8080
      protocol: TCP
      targetPort: 8080
    - name: mcp
      port: 8000
      protocol: TCP
      targetPort: 8000

odoo:
  url: https://torchmedia-odoo.woowtech.io
  db: torchmedia
  username: admin
  timeout: "30"
  verifySsl: ""

secrets:
  create: false
  # This pod carries the password as a PLAIN env value. The real value is
  # NOT here: pass it at takeover time with
  #   --set-string secrets.plainPassword="$(kubectl -n torchmedia get deploy mcp-odoo \
  #      -o jsonpath='{.spec.template.spec.containers[0].env[?(@.name=="ODOO_PASSWORD")].value}')"
  plainPassword: "__ODOO_PASSWORD__"
  existingSecret: mcp-odoo-secrets
  passwordKey: odoo-password

server:
  envOrder:
    - MCP_ADMIN_CONFIG
    - ODOO_URL
    - ODOO_DB
    - ODOO_USERNAME
    - ODOO_PASSWORD
    - ODOO_TIMEOUT
    - MCP_ALLOWED_HOSTS
    - MCP_ALLOW_REMOTE_HTTP
    - ODOO_MCP_ENABLE_WRITES
    - ODOO_MCP_ALLOW_DIRECT_WRITES
    - ODOO_MCP_ELICIT_WRITES
    - ODOO_MCP_ALLOW_UNKNOWN_METHODS
    - MCP_CHATTER_DIRECT
    - ODOO_MCP_MAX_ATTACHMENT_BYTES
    - ODOO_MCP_POLICY_FILE
  allowedHosts:
    - torchmedia-mcp-odoo.woowtech.io
    - localhost
    - 127.0.0.1
    - mcp-odoo-proxy.torchmedia.svc.cluster.local
    - mcp-odoo.torchmedia.svc.cluster.local
  enableWrites: "1"
  allowDirectWrites: "1"
  allowUnknownMethods: "1"
  elicitWrites: "0"
  maxAttachmentBytes: "16777216"
  chatterDirect: "1"
  allowRemoteHttp: "1"
  applyPatches: false
  ports:
    - containerPort: 8080
      name: admin
    - containerPort: 8000
      name: mcp
  # The env var is set but the flag is NOT on the command line.
  commandAllowRemoteHttp: false
  inProcessAdmin:
    enabled: true
    port: 8080
  livenessProbe:
    failureThreshold: 3
    initialDelaySeconds: 30
    periodSeconds: 30
    successThreshold: 1
    tcpSocket:
      port: 8080
    timeoutSeconds: 1
  readinessProbe:
    failureThreshold: 3
    initialDelaySeconds: 10
    periodSeconds: 10
    successThreshold: 1
    tcpSocket:
      port: 8080
    timeoutSeconds: 1
  # Verbatim: this tenant mounts the patch ConfigMap at /opt/patches and
  # lists it LAST, which no rule in the chart derives.
  volumeMountsOverride:
    - mountPath: /app/odoo_mcp_policy.json
      name: policy
      subPath: odoo_mcp_policy.json
    - mountPath: /data
      name: data
  volumesOverride:
    - configMap:
        defaultMode: 420
        name: mcp-odoo-policy
      name: policy
    - configMap:
        defaultMode: 420
        name: mcp-admin-config
      name: config-src
    - name: data
      persistentVolumeClaim:
        claimName: mcp-admin-data

policy:
  enabled: true
  mountPath: /app/odoo_mcp_policy.json

proxy:
  enabled: true
  volumeName: config
  # the live proxy Service names its port
  servicePortName: proxy
  resources:
    limits:
      cpu: 100m
      memory: 64Mi
    requests:
      cpu: 10m
      memory: 16Mi
  podAnnotations:
    kubectl.kubernetes.io/restartedAt: "2026-07-20T19:31:36+08:00"

initConfig:
  enabled: true
  existingConfigMap: mcp-admin-config
  # A third vintage of the seed command. Carried verbatim.
  command:
    - sh
    - -c
    - if [ ! -f /data/config.json ]; then cp /config-src/config.json /data/config.json;
      fi
  # init image: busybox:latest

persistence:
  enabled: true
  emptyDir: false
  existingClaim: mcp-admin-data
